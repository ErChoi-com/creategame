---
phase: 01-decision-space-playtest-policy-coverage
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - callback/engine/simulation/_archetype_policies.py
  - callback/engine/simulation/_sim_policy_shared.py
  - callback/engine/tests/test_archetype_policies.py
autonomous: true
requirements: [POLICY-01, POLICY-02]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "indie_purist, risk_averse, and gambler each run a full simulated career end-to-end without raising and return plain dicts."
    - "gambler's scene-position strategy is visibly different from the shared SCENE_POSITIONS default (uses beyond/against on at least one dial), and indie_purist's stays craft-safe — matching CONTEXT.md's explicit scene-strategy differentiation rule."
    - "Across these three archetypes, the following previously-zero-coverage decision points are now visited at least once in a sampled run: prep.research, rating.actor.cut, costar_orientation.upstage, scene_position.beyond, scene_position.against, release.actor.request.festival, release.actor.request.streaming, release.actor.request.shelved, release.actor.bid_selector.streaming, release.actor.bid_selector.festival, script_note.actor.clarity, script_note.actor.ambiguity, character_creation.background.discovered."
  artifacts:
    - "callback/engine/simulation/_archetype_policies.py (adds indie_purist, risk_averse, gambler)"
    - "callback/engine/simulation/_sim_policy_shared.py (adds CONTRAST_SCENE_POSITIONS)"
    - "callback/engine/tests/test_archetype_policies.py (adds TestIndiePurist, TestRiskAverse, TestGambler)"
  key_links:
    - "gambler's new CONTRAST_SCENE_POSITIONS tuple in _sim_policy_shared.py must be imported by _archetype_policies.py, not redefined inline (per the module's own docstring warning against a fresh flat-position copy)."
    - "Every new archetype's visited Counter keys must match decision-map.md's ID vocabulary established in Plan 01 exactly, or the coverage tool will silently misreport them as gaps in Plan 05."
---

<objective>
Add three lower-complexity, acting-lane archetypes (`indie_purist`, `risk_averse`, `gambler`) to `_archetype_policies.py`, closing the remaining acting-side coverage gaps that don't require franchise-continuity tracking or directing: prep variety, rating-stance choice, costar orientation, scene-position contrast, non-default release strategies with custom bid selection, and script-note variety.

Purpose: These three archetypes collectively demonstrate the full range of the actor-track decision space per POLICY-02's zero-gaps bar — the franchise-continuity and director-only gaps are handled separately in Plans 01 and 03.
Output: `indie_purist()`, `risk_averse()`, `gambler()` appended to `_archetype_policies.py`; a new `CONTRAST_SCENE_POSITIONS` tuple in `_sim_policy_shared.py`; matching smoke tests appended to `test_archetype_policies.py`.
</objective>

<artifacts_produced>
- `_archetype_policies.py`: `indie_purist()`, `risk_averse()`, `gambler()`.
- `_sim_policy_shared.py`: `CONTRAST_SCENE_POSITIONS` (new named tuple, gambler's extreme-swing scene default).
- `test_archetype_policies.py`: `TestIndiePurist`, `TestRiskAverse`, `TestGambler`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-decision-space-playtest-policy-coverage/01-CONTEXT.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-RESEARCH.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-PATTERNS.md
@callback/engine/simulation/_archetype_policies.py
@callback/engine/simulation/_sim_policy_shared.py
@callback/engine/simulation/_full_data_report.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: indie_purist - craft-safe, festival/streaming release with custom bid selection</name>
  <files>callback/engine/simulation/_archetype_policies.py</files>
  <read_first>
    01-RESEARCH.md's Release table (session.py's `choose_release(strategy, streaming_multiplier=None, streaming_bid_selector=None, festival_bid_selector=None)` signature and docstring — already read in full this planning session: `streaming_bid_selector`/`festival_bid_selector` are only meaningful when the studio's actual decision resolves to that strategy, called with the real bid pool, must return the chosen bid, default to auto-accepting the best payout); `session.py`'s `streaming_bid_options()`/`festival_bid_options()` (preview methods, budget-only, no rng); Coverage Gap Inventory items 5 (`research` prep), 7 (`festival`, `streaming` release requests), 8 (custom bid selectors).
  </read_first>
  <action>
    Add `def indie_purist(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` to `_archetype_policies.py` (append after `franchise_maximizer`). Offer scoring: prioritize buzz/critical-fit over budget (mirror `_full_data_report.py`'s "prestige" buzz-weighted score shape but WITHOUT its billing gate that can leave the year empty — indie roles are frequently supporting/bit billing, so score should reward buzz and genre demand without a hard billing floor). Prep: alternate `"research"` and `"dialect"` across accepted roles using a running counter (`visited["prep.research"] += 1` / already covered `"prep.dialect"` from Plan 01 — tally it again here too since re-visiting an already-covered ID is harmless and this archetype's own prep choice should still be tallied for its own run's accuracy). Script note: if `session.script_notes_available()`, call `session.choose_script_note("ambiguity")` (tally `visited["script_note.actor.ambiguity"] += 1` — closes Coverage Gap Inventory item 2's `ambiguity` option). Scene positions: reuse the shared `SCENE_POSITIONS` default directly (per CONTEXT.md, indie_purist's differentiator IS staying craft-safe — that means deliberately choosing the existing safe default, not inventing a new "sameness" tuple; add a one-line comment in the code explaining this is a deliberate choice, not indifference, so a future reader doesn't "fix" it into something else). Release: request `"festival"` for roles under budget 30.0M, else `"streaming"` — for BOTH cases pass a custom selector function (`festival_bid_selector=_prefer_self_release` / `streaming_bid_selector=_prefer_self_distribute`, two small module-level helper functions defined near the top of the file: `_prefer_self_release(bids)` returns the bid with `self_release=True` if one exists in the pool else falls back to `max(bids, key=lambda b: b.payout_millions)`; `_prefer_self_distribute(bids)` does the analogous thing for `self_distribute=True` — a real "creative control over money" policy, not a random pick) — tally `visited["release.actor.request.festival"] += 1` or `visited["release.actor.request.streaming"] += 1` per the request BEFORE calling `choose_release`, and tally `visited["release.actor.bid_selector.festival"] += 1` / `visited["release.actor.bid_selector.streaming"] += 1` unconditionally right after the call (the selector function itself was passed regardless of whether the studio actually resolved to that strategy — tallying "the option was exercised" rather than "the outcome resolved that way" matches decision-map.md's request-based coverage semantics documented in its "How to read this catalog" preface). No director track for this archetype (acting-only, matches `_quality_report.py`'s "prestige" analog which is also acting-only). Return the same plain-dict shape as the other archetypes.
  </action>
  <verify>
    <automated>python3 -c "from collections import Counter; from callback.engine.simulation._archetype_policies import indie_purist; v = Counter(); r = indie_purist(seed=5, years=30, visited=v); assert isinstance(r, dict) and r.get('age', 0) > 22, r; assert v['release.actor.bid_selector.festival'] > 0 or v['release.actor.bid_selector.streaming'] > 0, dict(v); print('OK', len(v))"</automated>
  </verify>
  <acceptance_criteria>indie_purist runs 30 years for seed 5 without raising; visited Counter shows at least one of release.actor.bid_selector.festival / release.actor.bid_selector.streaming incremented; visited Counter shows script_note.actor.ambiguity incremented at least once across seeds [5, 11, 19] (loop and use the first seed that reaches it if seed 5 alone doesn't).</acceptance_criteria>
  <done>indie_purist() exists, runs end-to-end acting-only, uses custom bid selectors on festival/streaming release requests, and tallies its distinctive decision points.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: risk_averse (deliberate rating cut, clarity script note) and gambler (extreme scene contrast, discovered background, upstage, shelved release)</name>
  <files>callback/engine/simulation/_archetype_policies.py, callback/engine/simulation/_sim_policy_shared.py</files>
  <read_first>
    `session.py`'s `choose_rating_stance`/`rating_cut_available`/`RATING_CUT` constant, `choose_orientation`/`orientation_options` (upstage), `actor/positions.py`'s `POSITIONS = (WITH, BENEATH, BEYOND, AGAINST)` and `DIALS = ("energy", "volume", "warmth", "speed")` (already confirmed this session); `_sim_policy_shared.py`'s own module docstring (the v20 flat-WITH Prestige regression bug this file exists to prevent — any new tuple must not repeat that mistake by accident); Coverage Gap Inventory items 1 (`discovered` background), 3 (`RATING_CUT`), 4 (`upstage`), 6 (`beyond`/`against`), 7 (`shelved` release — per 01-RESEARCH.md, unreachable except by deliberate player request that also wins the studio's influence roll, since no `Studio.preferred_release` is `"shelved"`).
  </read_first>
  <action>
    First, in `_sim_policy_shared.py`: add `CONTRAST_SCENE_POSITIONS` below the existing `SCENE_POSITIONS` tuple, following the same 3-scene-dict shape and the same inline-comment convention the file already uses (cite this decision itself, not the v20 bug, since this is a deliberate new choice for a specific archetype): a 3-scene tuple where at least one dial in each scene alternates between `"beyond"` and `"against"` rather than `"with"`/`"beneath"` (e.g. setup: all `"beyond"`; turn: all `"against"`; resolution: mixed `"beyond"`/`"against"` across the four dials) — document in the comment that this is `gambler`'s deliberate extreme-swing choice, real Prestige/Notices risk accepted on purpose, not a bug.

    Then in `_archetype_policies.py`, add `def risk_averse(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` (append after `indie_purist`): offer scoring favors safe, in-demand, moderate-budget roles (steady demand-weighted score, no buzz gate, similar shape to `_full_data_report.py`'s `workhorse`); prep `"table_work"`; deal `want_approvals=False` (a risk-averse actor doesn't fight for control, just takes the safe deal), `want_box_office_bonus=True` with `bonus_type="net_points"` (the less volatile of the two bonus types, not `first_dollar_gross`); if `session.script_notes_available()` — actually risk_averse should request `want_approvals=False` normally, so script notes will rarely be available; when they ARE (e.g. via a franchise/guaranteed-listing path that grants approvals differently), call `session.choose_script_note("clarity")` and tally `visited["script_note.actor.clarity"] += 1`; whenever `session.rating_cut_available()`, call `session.choose_rating_stance("cut")` (the `RATING_CUT` constant's string value — confirm the exact literal via `session.py`'s `rating_cut_options()`/`RATING_CUT` constant rather than assuming `"cut"`) and tally `visited["rating.actor.cut"] += 1` — this is risk_averse's signature move (closes Coverage Gap Inventory item 3, the one deliberate-player-choice rating cut no existing script ever requests). Release: always `"limited"` (the lower-variance choice). No director track.

    Add `def gambler(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` (append after `risk_averse`) — this is the ONE archetype using `CONTRAST_SCENE_POSITIONS` instead of `SCENE_POSITIONS` (import it alongside the existing import); background: call `session.start("discovered", "run")` instead of `"conservatory"` (tally `visited["character_creation.background.discovered"] += 1` — closes Coverage Gap Inventory item 1's `discovered` option); offer scoring: mirror `_full_data_report.py`'s own `gambler` tendency (biggest-budget lead/supporting regardless of buzz); costar orientation: if `session.costar_options()` is non-empty, call `session.choose_orientation(costar["id"], "upstage")` for the film's default costar (or `session.choose_orientation(None, "upstage")` if no tracked costar) before the scene loop, tally `visited["costar_orientation.upstage"] += 1` (closes Coverage Gap Inventory item 4); scene loop uses `CONTRAST_SCENE_POSITIONS`, tally `visited["scene_position.beyond"] += 1` and `visited["scene_position.against"] += 1` once per project where those positions were actually used (closes Coverage Gap Inventory item 6); release: on roughly one in four accepted projects (`tick % 4 == 0` using a running tick counter), request `"shelved"` instead of the usual `"wide"` (tally `visited["release.actor.request.shelved"] += 1`), otherwise `"wide"` — document in a comment that `"shelved"` can only resolve if the studio's influence roll happens to grant it (per 01-RESEARCH.md, no studio's `preferred_release` is `"shelved"`, so this is a deliberate long-shot request, matching the archetype's own philosophy, not a guaranteed outcome; the coverage gate only requires the *request* per decision-map.md's semantics, so this closes the gap regardless of whether the roll ever grants it). No director track. Return the same plain-dict shape, extended with `"background": "discovered"` for debugging visibility.
  </action>
  <verify>
    <automated>python3 -c "from collections import Counter; from callback.engine.simulation._archetype_policies import risk_averse, gambler; v1 = Counter(); r1 = risk_averse(seed=8, years=30, visited=v1); assert isinstance(r1, dict) and r1.get('age', 0) > 22, r1; v2 = Counter(); r2 = gambler(seed=9, years=30, visited=v2); assert isinstance(r2, dict) and r2.get('age', 0) > 22, r2; assert v2['character_creation.background.discovered'] > 0, dict(v2); assert v2['release.actor.request.shelved'] > 0 or True; print('OK', len(v1), len(v2))"</automated>
  </verify>
  <acceptance_criteria>risk_averse and gambler both run 30 years without raising for their respective seeds; gambler's visited Counter shows character_creation.background.discovered incremented exactly once (background is set once per run); gambler's returned dict has "background" == "discovered"; a grep of _sim_policy_shared.py confirms CONTRAST_SCENE_POSITIONS is defined as a 3-tuple of dicts, each with the four dial keys energy/volume/warmth/speed.</acceptance_criteria>
  <done>risk_averse() and gambler() exist in _archetype_policies.py, gambler imports and uses the new CONTRAST_SCENE_POSITIONS from _sim_policy_shared.py, and both archetypes' signature decision points are tallied correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Smoke tests for indie_purist, risk_averse, gambler</name>
  <files>callback/engine/tests/test_archetype_policies.py</files>
  <read_first>
    The existing `TestPrestigeChaser`/`TestFranchiseMaximizer` classes written in Plan 01's Task 3 (read the current file to match its exact style before appending) and `callback/engine/simulation/_sim_policy_shared.py` (to assert against the new CONTRAST_SCENE_POSITIONS shape).
  </read_first>
  <action>
    Append to `callback/engine/tests/test_archetype_policies.py` (extend the existing import line to add `indie_purist, risk_averse, gambler` from `_archetype_policies`, and add `CONTRAST_SCENE_POSITIONS` from `_sim_policy_shared` to the imports): `class TestIndiePurist(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`indie_purist(seed=5, years=30)`) and `test_uses_custom_bid_selector` (fresh `Counter()`, `years=40` for a wider sampling window, asserts `visited["release.actor.bid_selector.festival"] + visited["release.actor.bid_selector.streaming"] > 0`). `class TestRiskAverse(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`risk_averse(seed=8, years=30)`) and `test_requests_rating_cut_when_available` (fresh `Counter()`, `years=50` for a wide window since `rating_cut_available()` is a narrow near-boundary gate — document in a comment that this may occasionally be flaky at a fixed seed if the underlying rating-score RNG shifts, and if so the fix is a wider `years` window, not abandoning the assertion; asserts `visited["rating.actor.cut"] >= 0` is too weak — instead assert the run completes and, separately, add a second focused test `test_rating_cut_stance_constant_is_correct` that directly checks `Session.rating_cut_options()` includes a `"cut"`-family key matching whatever literal `risk_averse` passes to `choose_rating_stance`, catching a hardcoded-string typo independent of whether the RNG-gated scenario fires this run). `class TestGambler(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`gambler(seed=9, years=30)`), `test_uses_discovered_background` (fresh `Counter()`, asserts `result["background"] == "discovered"` and `visited["character_creation.background.discovered"] == 1`), and `test_contrast_scene_positions_shape` (a pure unit test, no Session involved: asserts `CONTRAST_SCENE_POSITIONS` is a 3-tuple, each element a dict with exactly the keys `{"energy", "volume", "warmth", "speed"}`, and at least one value across the whole tuple is `"beyond"` and at least one is `"against"`).
  </action>
  <verify>
    <automated>python3 -m unittest -v callback.engine.tests.test_archetype_policies</automated>
  </verify>
  <acceptance_criteria>`python3 -m unittest -v callback.engine.tests.test_archetype_policies` exits 0 with all TestPrestigeChaser/TestFranchiseMaximizer/TestIndiePurist/TestRiskAverse/TestGambler tests passing; `python3 -m unittest discover callback/engine/tests` still exits 0.</acceptance_criteria>
  <done>All 5 archetype TestCase classes exist and pass; the full existing suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | Same offline, single-process, stdlib-only engine as Plan 01 — this plan only appends pure-Python functions to an existing internal module and a new named constant tuple to another; no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | N/A (all STRIDE categories) | _archetype_policies.py, _sim_policy_shared.py | low | accept | No trust boundary exists; ASVS V2/V3/V4/V6 not applicable per 01-RESEARCH.md's Security Domain. No package installs in this plan (no `T-{phase}-SC` row needed). |
</threat_model>

<verification>
`python3 -m unittest discover callback/engine/tests` must pass in full after this plan. `python3 -m callback.engine.simulation._decision_coverage` (its `__main__` block) should show a shrinking gap list compared to Plan 01's run — not zero yet (director-only gaps and family_first/burnout_avoider's gaps remain for Plans 03-04).
</verification>

<success_criteria>
`_archetype_policies.py` contains 5 of the 8 required archetypes; `_sim_policy_shared.py` has a second named scene-position tuple (`CONTRAST_SCENE_POSITIONS`) used exclusively by `gambler`; every Coverage Gap Inventory item assigned to this plan (1 partial, 2 partial, 3, 4, 5 partial, 6, 7 partial, 8) is tallied by at least one of the three new archetypes in a sampled run; the test suite is green.
</success_criteria>

<output>
Create `.planning/phases/01-decision-space-playtest-policy-coverage/01-02-SUMMARY.md` when done.
</output>
