---
phase: 01-decision-space-playtest-policy-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - callback/docs/design/decision-map.md
  - callback/engine/simulation/_decision_coverage.py
  - callback/engine/simulation/_archetype_policies.py
  - callback/engine/tests/test_archetype_policies.py
  - callback/engine/tests/test_decision_coverage.py
autonomous: true
requirements: [MAP-01, POLICY-01, POLICY-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A developer reading callback/docs/design/decision-map.md can find every mechanically-branching decision point cataloged in this session's RESEARCH.md inventory, each row citing its source module/function and (where the module has one) its design-doc section."
    - "The `ambition` character-creation field is explicitly documented as excluded (zero mechanical effect, confirmed by grep of self.ambition in session.py) rather than silently missing from the catalog."
    - "Running prestige_chaser(seed, years) end-to-end for a full simulated career completes without raising and returns a plain dict (no Role/ActorState/ReceptionResult/StandingModel leaking out)."
    - "The coverage tool's check_coverage(visited) correctly reports IDs present in decision-map.md but absent from a Counter, and reports none of the IDs prestige_chaser actually visited."
  artifacts:
    - callback/docs/design/decision-map.md
    - callback/engine/simulation/_decision_coverage.py
    - "callback/engine/simulation/_archetype_policies.py (prestige_chaser, franchise_maximizer)"
    - callback/engine/tests/test_archetype_policies.py
    - callback/engine/tests/test_decision_coverage.py
  key_links:
    - "Each archetype function's visited Counter keys must exactly match the ID column values in decision-map.md, or check_coverage() will silently misreport gaps."
    - "_decision_coverage.py's check_coverage() reads its expected-ID set by parsing decision-map.md's ID column at call time (single source of truth) — it must never hardcode a duplicate ID list that can drift from the doc."
    - "Every new module imports only from callback.engine.simulation.session.Session (the one legal engine seam) — no Role/ActorState/ReceptionResult/StandingModel import anywhere in these 5 files."
---

<objective>
Establish the ground-truth decision-map catalog (MAP-01) and stand up the archetype-policy + coverage-check infrastructure this whole phase builds on (POLICY-01, POLICY-02), proven end-to-end with the first archetype (`prestige_chaser`, both acting and directing tracks) wired through `Session`, plus a second archetype (`franchise_maximizer`) that begins closing the franchise-continuity coverage gaps no existing report script touches.

Purpose: Nothing in Plans 02-05 can proceed without the decision-map's ID vocabulary and the coverage tool's `check_coverage()`/`run_with_coverage()` contract existing first — this plan is the tracer slice the rest of the phase expands from.
Output: `decision-map.md` (full catalog), `_decision_coverage.py` (coverage tool), `_archetype_policies.py` with `prestige_chaser()` and `franchise_maximizer()`, and the two new test files with their first TestCase classes.
</objective>

<artifacts_produced>
New symbols this plan introduces (the phase's full manifest lives here; later plans append to `_archetype_policies.py` and the two test files without re-declaring these):
- `callback/docs/design/decision-map.md` — the MAP-01 catalog, a standalone doc outside the part-00..16 series.
- `callback/engine/simulation/_decision_coverage.py` — `DECISION_MAP_PATH`, `load_expected_ids()`, `check_coverage()`, `run_with_coverage()`, `ARCHETYPE_NAMES` (all 8 archetype names), `run_full_coverage_check()`.
- `callback/engine/simulation/_archetype_policies.py` — `prestige_chaser()`, `franchise_maximizer()` (6 more functions added across Plans 02-04).
- `callback/engine/tests/test_archetype_policies.py` — `TestPrestigeChaser`, `TestFranchiseMaximizer`.
- `callback/engine/tests/test_decision_coverage.py` — `TestCheckCoverage` (unit), `TestCoverageIntegration` (runs the archetypes that exist so far).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-CONTEXT.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-RESEARCH.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-PATTERNS.md
@callback/engine/simulation/_full_data_report.py
@callback/engine/simulation/_sim_policy_shared.py
@callback/engine/simulation/session.py
@callback/docs/design/part-07-the-director.md
@callback/docs/design/creative-revamp-plan.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Decision-map catalog + coverage tool + first archetype (prestige_chaser), wired end-to-end</name>
  <files>callback/docs/design/decision-map.md, callback/engine/simulation/_decision_coverage.py, callback/engine/simulation/_archetype_policies.py</files>
  <read_first>
    01-RESEARCH.md's "Full Decision-Point Inventory" (the row content source, every entry already verified against session.py this research session), "Coverage Gap Inventory" (the 28-item checklist), "Gated Decision Points" table, and Pitfalls 1-5; 01-PATTERNS.md's per-file pattern assignments for all three files; `callback/engine/simulation/_full_data_report.py` (the exact call-order template — its `"prestige"` tendency branch is the direct precedent for prestige_chaser); `callback/engine/simulation/_sim_policy_shared.py` (SCENE_POSITIONS — reuse, do not redefine); `callback/docs/design/creative-revamp-plan.md` lines 1-10 (the doc-header convention for a standalone plan doc living outside the part-00..16 series — decision-map.md follows this same convention, not the numbered part-NN pattern).
  </read_first>
  <precondition>callback/docs/design/decision-map.md, callback/engine/simulation/_decision_coverage.py, and _archetype_policies.py do not yet exist (confirm with a directory listing before writing) — this task creates all three from scratch.</precondition>
  <action>
    Part A — `callback/docs/design/decision-map.md` (MAP-01, per the Decision Map decisions in 01-CONTEXT.md's Implementation Decisions section):
    Open with a short framing paragraph (matching creative-revamp-plan.md's convention of a standalone doc living outside the part-00..16 series, no YAML frontmatter, plain `#`/`##` headings), then one markdown table PER package-boundary section (Character Creation, Offer Board / Casting, The Deal, Script Notes, Rating Stance, Costar Orientation, Prep, The Shoot — Scene Positions, Release, Marketing Push, Multi-Picture Deals, Franchise Holdout, Spin-offs — Actor Track, Awards, Leverage — Agent/Scarcity, Rolodex Pulls, Life, Director Mode — one subsection per director decision-point group per 01-RESEARCH.md's own director-mode table rows). Every table uses this exact column order so the ID column is always first and always backtick-wrapped: `| ID | Decision Point | Options | Source (module/function) | Design section |`. One row per discrete option value (per CONTEXT.md's granularity decision — e.g. `prep.research` and `prep.dialect` are separate rows, not one `prep` row), using dot-separated lower_snake_case IDs following this scheme: `{category}.{subject}.{option}` — e.g. `character_creation.background.discovered`, `deal.box_office_bonus.first_dollar_gross`, `prep.research`, `scene_position.beyond`, `release.actor.request.festival`, `awards.actor.category.lead`, `director.dev_action.self_finance`, `director.franchise.reboot_pitch`. Transcribe every row from 01-RESEARCH.md's "Full Decision-Point Inventory" tables, cross-referencing each cited module's own docstring (they all open with a design-doc-section citation) to fill the Design section column accurately — re-grep the cited file/function if a line number looks stale rather than trusting 01-RESEARCH.md's line numbers verbatim (that document's own Metadata section flags this risk). Add a one-line callout under Character Creation explicitly excluding `ambition` (cite `session.py`'s `self.ambition` write/read sites, state it has zero mechanical effect, confirmed by grep) — no row, no ID, just prose per CONTEXT.md's "purely cosmetic... excluded" rule. Add a one-line callout under Awards noting `_quality_report.py`'s `run_awards_campaign(spend_millions=2.0)` call site is missing the required `category` argument and would raise `TypeError` the first time it fires — flagged as a known pre-existing bug in a superseded script, explicitly NOT fixed by this phase (this phase only adds new files; it does not modify `_quality_report.py`), and note that every new archetype in this phase must call `run_awards_campaign` with an explicit `category` argument to avoid repeating it. Add a short "How to read this catalog" preface explaining the coverage semantics from Pitfall 4: for any decision with a studio/engine-resolved outcome that can differ from the player's request (release strategy is the clear case), the row's ID represents the *requested* decision (the player-facing choice), which is what `_decision_coverage.py` counts as "covered" — resolved-outcome tallying is a secondary informative signal, not the gate. In that same preface, add a note per Pitfall 5: series projects (`project_type == "series"`) resolve through `_choose_release_series()`, not `choose_release()`, and have no release-strategy or box-office-bonus decision at all by design — a run-year that happens to be a series project contributing zero release-strategy tallies is not itself a coverage failure, since the tool only requires each ID to be visited at least once across the whole sampled run, not in every year it could theoretically apply.

    Part B — `callback/engine/simulation/_decision_coverage.py` (POLICY-02's verification tool, per the Coverage Verification decisions in 01-CONTEXT.md):
    Module docstring citing decision-map.md as its spec (not a design-doc section, since this module is a catalogue-consumer per 01-PATTERNS.md's "Design-doc citation" shared pattern), plain top-level functions, no classes (mirrors `simulation/verify.py`'s shape). Implement: `DECISION_MAP_PATH = Path(__file__).resolve().parents[2] / "docs" / "design" / "decision-map.md"` (resolves to `callback/docs/design/decision-map.md` from `callback/engine/simulation/_decision_coverage.py`); `load_expected_ids(path: Path = DECISION_MAP_PATH) -> frozenset[str]` — reads the file, and for every line matching a `|` followed by a backtick-wrapped token as the first cell, extracts that token as an ID (skip header/separator rows — a check that the extracted token isn't literally "ID" and isn't composed only of `-` characters is sufficient; no markdown-table parsing library needed); `check_coverage(visited, expected: frozenset[str] | None = None) -> list[str]` — returns the sorted list of expected IDs where `visited.get(id, 0) == 0` (defaults `expected` to `load_expected_ids()`); `run_with_coverage(policy_fn, seed: int, **kwargs) -> tuple[dict, Counter]` — creates a `Counter()`, calls `policy_fn(seed=seed, visited=that_counter, **kwargs)`, returns `(summary, counter)` (per 01-PATTERNS.md's recommended shape); `ARCHETYPE_NAMES = ("prestige_chaser", "franchise_maximizer", "indie_purist", "risk_averse", "gambler", "director_track", "family_first", "burnout_avoider")` (the full manifest — later plans add the remaining 6 functions to `_archetype_policies.py`; referencing all 8 names here now, resolved lazily via `getattr` only when actually called, is safe even though 6 don't exist yet); `run_full_coverage_check(seeds: list[int], years: int = 60) -> list[str]` — imports `_archetype_policies` (the module, not individual names), for each name in `ARCHETYPE_NAMES` and each seed calls `getattr(_archetype_policies, name)(seed=seed, years=years, visited=merged)` accumulating into one shared `Counter`, then returns `check_coverage(merged)` (this function is exercised for real only once every archetype exists — Plan 05 — but must be fully defined and correctly shaped now so its contract never changes later). Add an `if __name__ == "__main__":` CLI block modeled on `verify.py`'s printed-table style: run `run_full_coverage_check` at a couple of fixed seeds with `years=60`, print `<<< UNVISITED  {id}` per gap or `OK - zero gaps` if the list is empty (matches 01-PATTERNS.md's recommended report-output shape).

    Part C — `callback/engine/simulation/_archetype_policies.py` (POLICY-01, per the Playtest Policy Set decisions in 01-CONTEXT.md), `prestige_chaser()` only in this task:
    Module docstring citing decision-map.md (not a design-doc section, same reasoning as Part B) and naming all 8 archetypes this module will eventually hold. Import block: `from collections import Counter`; `from callback.engine.director import casting, shoot_style, development as director_development`; `from callback.engine.simulation.session import Session`; `from callback.engine.simulation._sim_policy_shared import SCENE_POSITIONS`; `from callback.engine.world.genre_cycle import genre_demand as world_genre_demand` — matching `_full_data_report.py`'s exact import shape, never importing `Role`, `ActorState`, `ReceptionResult`, or `StandingModel`. Write `def prestige_chaser(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` following `_full_data_report.py`'s `run(seed, "prestige", ...)` branch call-order exactly: `session.start("conservatory", "work")`, `session.become_director()`, then per-year `offer_board()` -> buzz/demand/billing score -> `accept()` -> `choose_deal(want_approvals=True, want_box_office_bonus=True, bonus_type=..., want_merchandising=...)` -> `choose_prep("physical_transformation")` -> scene loop using `SCENE_POSITIONS` -> `rating_cut_available()`/`choose_rating_stance` -> `request_marketing_push()` -> `choose_release(strategy)` -> the director-track block using `casting.YOUR_ROSTER`/`shoot_style.HEAVY_COVERAGE` -> `end_year()`. Create a local `Counter()` if `visited is None`. At every decision call site, increment the matching decision-map ID (e.g. `visited["character_creation.background.conservatory"] += 1` right after `session.start(...)`; `visited["deal.approvals"] += 1` when `want_approvals=True` is actually passed; `visited["release.actor.request.wide"] += 1` or the `"...limited"` variant per the `strategy` variable right before `choose_release`, AND `visited[f"release.actor.resolved.{result['release_label']}"] += 1` after, per Pitfall 4's dual-axis tally — informative only, not gated). Two required deviations from the plain `_full_data_report.py` "prestige" pattern, both closing real coverage gaps (RESEARCH.md Coverage Gap Inventory items 2, 12, 13, 14): (1) after `choose_deal(want_approvals=True, ...)`, if `session.script_notes_available()`, call `session.choose_script_note("your_part")` instead of `"whole_film"` and tally `visited["script_note.actor.your_part"] += 1` (closes gap item 2's `your_part` option — `whole_film` is already covered by every existing report script, no new coverage there); (2) whenever `session.awards_campaign_available()` returns true, call `categories = session.available_award_categories()`, deterministically rotate through them with a running `award_campaign_count` counter (`category = categories[award_campaign_count % len(categories)]`), call `session.run_awards_campaign(category=category, spend_millions=2.0, attempt_category_fraud=(award_campaign_count % 3 == 0))` (an explicit `category` kwarg fixes the exact bug documented in decision-map.md's Awards callout and in 01-RESEARCH.md's Pitfall 3 — never omit it), tally `visited[f"awards.actor.category.{category.lower()}"] += 1`, tally `visited["awards.actor.category_fraud"] += 1` whenever `attempt_category_fraud` was `True` this call, and increment `award_campaign_count`. For the director side of the "both tracks" pattern (director awards is a separate, director-only entry point — grep `session.py` for the director-side awards-campaign method near the actor `run_awards_campaign` definition, confirm its exact name and signature before calling it, since it is a distinct method from the actor one), call it whenever its own availability check returns true and tally `visited["awards.director.campaign"] += 1`. Return the same plain-dict summary shape `_full_data_report.py`'s `run()` returns (age, credits, standing, net_worth, franchise_status, etc. — read that function's final `return` block directly rather than inventing new fields).
  </action>
  <verify>
    <automated>python3 -c "from collections import Counter; from callback.engine.simulation._archetype_policies import prestige_chaser; from callback.engine.simulation._decision_coverage import check_coverage, load_expected_ids; v = Counter(); r = prestige_chaser(seed=1, years=15, visited=v); assert isinstance(r, dict) and r.get('age', 0) > 22, r; assert len(v) >= 5, dict(v); ids = load_expected_ids(); assert len(ids) >= 50, len(ids); gaps = check_coverage(v, ids); assert isinstance(gaps, list); print('OK', len(v), 'ids visited,', len(gaps), 'gaps remain')"</automated>
  </verify>
  <acceptance_criteria>decision-map.md contains at least 50 data rows whose first cell is a backtick-wrapped ID (a line-count grep of rows starting with a `|` followed by a backtick returns 50 or more); the Character Creation section contains a prose line mentioning "ambition" with zero `character_creation.ambition` rows anywhere in the file; the Awards section contains a prose line naming `_quality_report.py`; `load_expected_ids()` returns a frozenset containing no entry equal to the literal string "ID"; the verify command above prints "OK" with a nonzero visited-ID count and a gaps list.</acceptance_criteria>
  <done>decision-map.md exists with the full catalog and both callouts; _decision_coverage.py exists with load_expected_ids/check_coverage/run_with_coverage/run_full_coverage_check/ARCHETYPE_NAMES; _archetype_policies.py exists with a working prestige_chaser() that runs both tracks end-to-end, tallies against decision-map IDs, and calls run_awards_campaign (and its director-side counterpart) with an explicit category.</done>
  <reversibility rating="reversible">decision-map.md's location (callback/docs/design/decision-map.md, standalone doc outside the part-00..16 series) was already locked in 01-CONTEXT.md's Implementation Decisions — not re-litigated here. It is a plain new markdown file with no other file depending on its path except this phase's own new Python modules (which reference it via a computed relative path), so moving it later would be a one-line change, not a one-way door.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Second archetype - franchise_maximizer (franchise-continuity coverage)</name>
  <files>callback/engine/simulation/_archetype_policies.py</files>
  <read_first>
    01-RESEARCH.md's "Gated Decision Points" table (the exact thresholds: FRANCHISE_INDISPENSABILITY_HOLDOUT_THRESHOLD=30.0, SPINOFF_INDISPENSABILITY_THRESHOLD=55.0, MULTI_PICTURE_MIN_STANDING=40.0) and the "Practical implication for the planner" paragraph immediately below it (no existing script tracks franchise_id/installment_number continuity across years — this is genuinely new logic); 01-PATTERNS.md's "Franchise-continuity deviation (for franchise-maximizer)" note; `session.py`'s `holdout_available`/`request_holdout`, `multi_picture_deal_available`/`multi_picture_deal_terms`/`sign_multi_picture_deal`/`break_multi_picture_deal`, and `spinoff_options`/`launch_spinoff` methods — exact signatures already confirmed by direct read this planning session (holdout_available takes no args and reads self._role internally right after accept(); sign_multi_picture_deal(film_count: int) -> dict; launch_spinoff(franchise_id: str) -> str).
  </read_first>
  <action>
    Add `def franchise_maximizer(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` to `_archetype_policies.py` (the file created in Task 1 — append after `prestige_chaser`, do not modify that function). This archetype's offer-scoring closure is genuinely new logic (not copied from any existing tendency's `best = max(available, key=...)`): each year, after computing `available = [o for o in board if o["available"]]`, first check for any offer whose `o["franchise_id"]` matches a franchise this run has already held (track a local `held_franchise_ids: set[str]` populated whenever `accept()` is called on a franchised role) — if one exists, prefer it outright (continuity beats a fresh, unrelated franchise) by selecting the first matching offer; otherwise fall back to scoring by whether `o.get("franchise_id")` is not None (any franchise start beats a one-off), then billing/budget, matching `_full_data_report.py`'s `gambler` tendency's scoring shape for the fallback case. Deal: `want_approvals=True`, `bonus_type` prefers `first_dollar_gross`, `want_merchandising=session.merchandising_available()` (tally `visited["deal.merchandising"] += 1` whenever `True` is actually passed). Prep: use `"dialect"` (closes RESEARCH.md Coverage Gap Inventory item 5's `dialect` half — `research` is `indie_purist`'s job in Plan 02; do not duplicate it here) and tally `visited["prep.dialect"] += 1`. Immediately after every `accept()` on a franchised role (`best.get("franchise_id") is not None`), before `choose_deal`: if `session.multi_picture_deal_available()`, call `session.multi_picture_deal_terms(3)` then `session.sign_multi_picture_deal(3)` (tally `visited["multi_picture_deal.terms_preview"] += 1` and `visited["multi_picture_deal.sign"] += 1`) — guard with a local `signed_deal: bool` flag so this only happens once per run. On a later year, once at least 3 in-game years have passed since signing, call `session.break_multi_picture_deal()` exactly once (tally `visited["multi_picture_deal.break"] += 1`) to also exercise that path — guard so it fires at most once. If `session.holdout_available()` is `True` (installment 2+, indispensability >= 30.0 per the threshold above), call `session.request_holdout()` and tally `visited["franchise.holdout.request"] += 1`. For `session.spinoff_options()`, if any franchise clears `SPINOFF_INDISPENSABILITY_THRESHOLD` (55.0), call `session.launch_spinoff(franchise_id)` on the first eligible one found and tally `visited["franchise.spinoff.actor_launch"] += 1`. Scene positions: reuse the shared `SCENE_POSITIONS` default (this archetype's differentiator is franchise continuity, not scene choice, per CONTEXT.md's rule that non-differentiating archetypes reuse the shared default). Release: request `"wide"` when `best["budget_millions"] >= 50.0` else `"limited"`. Director track: skip entirely for this archetype (per CONTEXT.md, only `prestige_chaser` needs the "both tracks" acting-side variant; `director_track` in Plan 03 is the dedicated director archetype) — do not call `become_director()`. Return the same plain-dict shape as `prestige_chaser`, extended with `"held_franchise_ids": sorted(held_franchise_ids)` and `"multi_picture_deal_signed": signed_deal` for debugging visibility (plain data only).
  </action>
  <verify>
    <automated>python3 -c "from collections import Counter; from callback.engine.simulation._archetype_policies import franchise_maximizer; v = Counter(); r = franchise_maximizer(seed=3, years=40, visited=v); assert isinstance(r, dict) and r.get('age', 0) > 22, r; assert 'held_franchise_ids' in r; print('OK', len(v), 'ids visited')"</automated>
  </verify>
  <acceptance_criteria>franchise_maximizer runs 40 in-game years for at least 3 distinct seeds (1, 3, 42) without raising; the returned dict contains "held_franchise_ids" and "multi_picture_deal_signed" keys; visited Counter contains "prep.dialect" after the run.</acceptance_criteria>
  <done>franchise_maximizer() exists in _archetype_policies.py, runs end-to-end, and its franchise-continuity logic is exercised by at least one seed reaching a multi-picture deal signature within the verify command's 40-year window.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: First test files - smoke tests for both archetypes and coverage-tool correctness tests</name>
  <files>callback/engine/tests/test_archetype_policies.py, callback/engine/tests/test_decision_coverage.py</files>
  <read_first>
    `callback/engine/tests/test_full_career.py` (unittest shape, drives a multi-year loop then asserts on resulting state) and `callback/engine/tests/test_session.py` (Session-direct assertion style, one TestCase per feature area) — both already read this planning session; 01-VALIDATION.md's Per-Task Verification Map (rows 01-01-01, 01-01-02) and Wave 0 Requirements list, which name these exact two files.
  </read_first>
  <action>
    `callback/engine/tests/test_archetype_policies.py`: module docstring citing `_archetype_policies.py` and decision-map.md; `import unittest` plus `from collections import Counter` and `from callback.engine.simulation._archetype_policies import prestige_chaser, franchise_maximizer`. `class TestPrestigeChaser(unittest.TestCase):` with `test_runs_to_completion_without_raising` (calls `prestige_chaser(seed=1, years=15)`, asserts `result["age"] > 22` and `isinstance(result, dict)`) and `test_tallies_awards_and_your_part_script_note` (calls with a fresh `Counter()` passed as `visited`, runs `years=40` for a longer window to reliably reach the awards-spotlight threshold, asserts `visited["script_note.actor.your_part"] > 0`, and asserts at least one `awards.actor.category.*` key is present — do not assert on exact counts, engine RNG makes exact hit-rates non-deterministic across environments even at a fixed seed if the seed/rng-consumption pattern shifts). `class TestFranchiseMaximizer(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`franchise_maximizer(seed=3, years=40)`) and `test_tracks_franchise_continuity` (asserts `result["held_franchise_ids"]` is a list, and — for at least one of seeds `[3, 7, 21]`, looping and using the first one that reaches it — that `visited["multi_picture_deal.sign"] > 0`, documenting in a comment that franchise offers are randomly generated so a single seed is not guaranteed to reach the multi-picture-deal threshold within 40 years).

    `callback/engine/tests/test_decision_coverage.py`: module docstring citing `_decision_coverage.py` and decision-map.md; imports `unittest`, `Counter`, and `check_coverage`, `load_expected_ids`, `run_with_coverage` from `_decision_coverage`, plus `prestige_chaser` from `_archetype_policies`. `class TestCheckCoverage(unittest.TestCase):` — pure unit tests with a small synthetic `expected = frozenset({"a", "b", "c"})`: `test_reports_ids_with_zero_count` (`visited = Counter({"a": 2})`; `check_coverage(visited, expected) == ["b", "c"]`), `test_reports_empty_list_when_all_visited` (`visited = Counter({"a": 1, "b": 1, "c": 1})`; asserts the result is `[]`), `test_load_expected_ids_excludes_header_row` (calls `load_expected_ids()` against the real decision-map.md and asserts `"ID"` is not a member and the set has at least 50 members — cross-checks Task 1's acceptance criteria from the test side too). `class TestCoverageIntegration(unittest.TestCase):` — `test_prestige_chaser_visits_are_all_known_ids` (`summary, visited = run_with_coverage(prestige_chaser, seed=1, years=15)`; `expected = load_expected_ids()`; assert every key in `visited` is a member of `expected` — this catches a typo'd Counter key in the archetype code that would otherwise silently never register as covered).
  </action>
  <verify>
    <automated>python3 -m unittest -v callback.engine.tests.test_archetype_policies callback.engine.tests.test_decision_coverage</automated>
  </verify>
  <acceptance_criteria>`python3 -m unittest -v callback.engine.tests.test_archetype_policies callback.engine.tests.test_decision_coverage` exits 0 with all tests reported OK; `python3 -m unittest discover callback/engine/tests` still exits 0 (confirms this additive-only plan touched no existing test).</acceptance_criteria>
  <done>Both new test files exist, both pass, and the full existing 117+-test suite still passes unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | This phase adds an offline, single-process, stdlib-only Markdown doc and Python modules to an existing engine with no auth, network, session, or crypto surface (per 01-RESEARCH.md's Security Domain and 01-VALIDATION.md's Security Note). Seeds are internal integers generated by the caller; decision-map.md is a static, developer-authored reference doc, not user input. No new external/untrusted input surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | N/A (all STRIDE categories) | decision-map.md, _decision_coverage.py, _archetype_policies.py | low | accept | No trust boundary exists for this offline, single-player, stdlib-only simulation engine (ASVS V2 Authentication, V3 Session Management, V4 Access Control, V6 Cryptography: not applicable per 01-RESEARCH.md's Applicable ASVS Categories table). V5 Input Validation is marginally relevant only if a future CLI wrapper is added around the coverage tool — out of this phase's scope; accepted as a non-issue for the code this plan actually writes. |
</threat_model>

<verification>
Run `python3 -m unittest discover callback/engine/tests` after this plan's tasks complete — the full existing suite plus the two new files must all pass. Additionally run `python3 -m callback.engine.simulation._decision_coverage` directly (its `__main__` block) to confirm it does not crash even though only 2 of 8 `ARCHETYPE_NAMES` exist yet — expect it to report many gaps at this stage (Plans 02-05 close them), not zero.
</verification>

<success_criteria>
decision-map.md exists as the MAP-01 ground-truth catalog with the ambition and _quality_report.py callouts; `_decision_coverage.py` provides the full coverage-tool contract (`check_coverage`, `run_with_coverage`, `run_full_coverage_check`, `load_expected_ids`) that Plans 02-05 build on without modification; `_archetype_policies.py` contains 2 of the 8 required archetypes (`prestige_chaser`, `franchise_maximizer`), both wired end-to-end through `Session` and both tallying against decision-map.md's ID vocabulary; both new test files exist and pass; the full existing test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/01-decision-space-playtest-policy-coverage/01-01-SUMMARY.md` when done.
</output>
