---
phase: 01-decision-space-playtest-policy-coverage
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - callback/engine/simulation/_archetype_policies.py
  - callback/engine/tests/test_archetype_policies.py
autonomous: true
requirements: [POLICY-01, POLICY-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "family_first and burnout_avoider each run a full simulated career end-to-end without raising and return plain dicts, completing the 8-of-8 archetype minimum required by POLICY-01."
    - "family_first's background is family_money and it exercises all three previously-uncovered rolodex interaction verbs (show_up, read_agenda, vouch) at least once each in a sampled run."
    - "burnout_avoider's background is regional_stage and it exercises disappear() (banking Scarcity) at least once in a sampled run."
  artifacts:
    - "callback/engine/simulation/_archetype_policies.py (adds family_first, burnout_avoider — completes all 8 archetypes)"
    - "callback/engine/tests/test_archetype_policies.py (adds TestFamilyFirst, TestBurnoutAvoider)"
  key_links:
    - "Both archetypes' visited Counter keys must match decision-map.md's ID vocabulary from Plan 01 exactly."
    - "With this plan's completion, _archetype_policies.py's ARCHETYPE_NAMES manifest (defined in _decision_coverage.py, Plan 01) is fully satisfied — every name in that tuple now resolves to a real function, unblocking Plan 05's run_full_coverage_check()."
---

<objective>
Add the final two archetypes, `family_first` and `burnout_avoider`, completing the POLICY-01 minimum of 8 distinct scripted playtest policies and closing the last acting-side coverage gaps: the `family_money` background, the three unused rolodex interaction verbs (`show_up`, `read_agenda`, `vouch`), the `regional_stage` background, and `disappear()` (banking Scarcity).

Purpose: With all 8 archetypes in place, `_decision_coverage.py`'s `run_full_coverage_check()` (built in Plan 01) can finally be exercised for real — Plan 05 uses it to confirm (or find and close) any remaining gaps.
Output: `family_first()` and `burnout_avoider()` appended to `_archetype_policies.py`; `TestFamilyFirst` and `TestBurnoutAvoider` appended to `test_archetype_policies.py`.
</objective>

<artifacts_produced>
- `_archetype_policies.py`: `family_first()`, `burnout_avoider()` — the 7th and 8th archetypes, completing POLICY-01's minimum.
- `test_archetype_policies.py`: `TestFamilyFirst`, `TestBurnoutAvoider`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-decision-space-playtest-policy-coverage/01-CONTEXT.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-RESEARCH.md
@callback/engine/simulation/_archetype_policies.py
@callback/engine/simulation/_full_data_report.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: family_first (family_money background, full rolodex interaction variety) and burnout_avoider (regional_stage background, disappear())</name>
  <files>callback/engine/simulation/_archetype_policies.py</files>
  <read_first>
    `callback/engine/simulation/_backgrounds.py` (BACKGROUND_TABLE — `family_money` starts with a real `MoneyState(net_worth=2.0)` and lower attributes; `regional_stage` starts at `REGIONAL_STAGE_START_AGE = 33`, not 22); `session.py`'s `interact(npc_id, action)` (action: "check_in" | "show_up" | "read_agenda" | "vouch" — already read in full this planning session, including that `"read_agenda"` has its own 50/50 rng-gated `served` outcome that credits a favour when it lands, and that every `interact` call costs one of the year's actor quarters, so a policy must budget for 3-4 quarters just to cycle all four verbs) and `rolodex_summary()`; `session.py`'s `disappear()` (spends the REST of the year's actor quarters at once, banks Scarcity via `accumulate_scarcity`, does not advance the calendar itself) and Coverage Gap Inventory items 1 (`family_money`, `regional_stage`), 15 (`disappear`), 16 (rolodex `show_up`/`read_agenda`/`vouch`).
  </read_first>
  <action>
    Add `def family_first(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` to `_archetype_policies.py` (append after `director_track`). Background: `session.start("family_money", "work")` (tally `visited["character_creation.background.family_money"] += 1` — closes Coverage Gap Inventory item 1's `family_money` option). Offer scoring: favors steady, moderate-risk work similar to `risk_averse` but with a lower budget/prestige bar (the money is already solved, per the background's own tagline — this archetype works for the craft and relationships, not the paycheck) — reuse a demand-weighted score without a buzz gate. Prep `"table_work"`; deal `want_approvals=False`, `want_box_office_bonus=True`; scene positions reuse the shared `SCENE_POSITIONS` default; release always `"limited"`. The archetype's real differentiator is its declined-board behavior: whenever the board yields nothing worth taking (mirroring the `else: session.decline_board()` branch every existing tendency already has), instead of only calling `interact(target["id"], "check_in")` in a loop (every existing script's exclusive pattern per Pitfall 2), explicitly cycle all four interaction verbs across the year's available `actor_quarters_remaining_this_year()` budget in a fixed rotation — `check_in`, `show_up`, `read_agenda`, `vouch` — against successive tracked NPCs from `rolodex_summary()` (falling back to repeating the rotation against the same NPC if fewer than 4 are tracked; skip a verb for a year with no tracked NPCs at all rather than erroring), tallying `visited["rolodex.interact.show_up"] += 1`, `visited["rolodex.interact.read_agenda"] += 1`, `visited["rolodex.interact.vouch"] += 1` each time those specific actions are actually called (closes Coverage Gap Inventory item 16 — `check_in` is already covered by every existing script, tally it too for this archetype's own accuracy but it closes no new gap). No director track. Return the same plain-dict shape.

    Add `def burnout_avoider(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` (append after `family_first`). Background: `session.start("regional_stage", "work")` (tally `visited["character_creation.background.regional_stage"] += 1` — closes Coverage Gap Inventory item 1's last option; note this background starts at age 33, not 22, so this archetype's own career naturally runs shorter within the same `years` budget — that's in-character, not a bug). Offer scoring: takes whatever reasonably fits (workhorse-style, no gate), but with a deliberate pacing rule: track a running `years_since_break: int`, and whenever it exceeds a threshold (e.g. 4 consecutive years worked without a break) AND `session.actor_quarters_remaining_this_year() > 0`, call `session.disappear()` instead of taking that year's offer (tally `visited["leverage.disappear"] += 1` — closes Coverage Gap Inventory item 15), reset `years_since_break` to 0, and skip the rest of that year's acting decisions (per `disappear()`'s own docstring, it spends the whole rest of the year's actor quarters). On years the archetype does work, use standard `table_work` prep and `"limited"` release, no approvals. No director track. Return the same plain-dict shape, extended with `"disappear_count": int` for debugging visibility.
  </action>
  <verify>
    <automated>python3 -c "from collections import Counter; from callback.engine.simulation._archetype_policies import family_first, burnout_avoider; v1 = Counter(); r1 = family_first(seed=17, years=40, visited=v1); assert isinstance(r1, dict) and r1.get('age', 0) > 22, r1; assert v1['character_creation.background.family_money'] == 1, dict(v1); assert v1['rolodex.interact.show_up'] > 0 and v1['rolodex.interact.read_agenda'] > 0 and v1['rolodex.interact.vouch'] > 0, dict(v1); v2 = Counter(); r2 = burnout_avoider(seed=23, years=40, visited=v2); assert isinstance(r2, dict) and r2.get('age', 0) > 32, r2; assert v2['character_creation.background.regional_stage'] == 1, dict(v2); assert v2['leverage.disappear'] > 0, dict(v2); print('OK', len(v1), len(v2))"</automated>
  </verify>
  <acceptance_criteria>family_first runs 40 years for seed 17 without raising, tallies family_money background exactly once, and tallies all three of show_up/read_agenda/vouch at least once; burnout_avoider runs 40 years for seed 23 without raising, tallies regional_stage background exactly once (starting age > 32), and tallies leverage.disappear at least once.</acceptance_criteria>
  <done>family_first() and burnout_avoider() exist in _archetype_policies.py — all 8 required archetypes now exist, matching _decision_coverage.py's ARCHETYPE_NAMES manifest from Plan 01 exactly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Smoke tests for family_first and burnout_avoider</name>
  <files>callback/engine/tests/test_archetype_policies.py</files>
  <read_first>
    The existing test classes in `test_archetype_policies.py` from Plans 01-03 (read the current file to match its exact style before appending).
  </read_first>
  <action>
    Append to `callback/engine/tests/test_archetype_policies.py` (extend the import line to add `family_first, burnout_avoider`): `class TestFamilyFirst(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`family_first(seed=17, years=40)`) and `test_exercises_full_rolodex_interaction_variety` (fresh `Counter()`, `years=40`, asserts `visited["rolodex.interact.show_up"] > 0`, `visited["rolodex.interact.read_agenda"] > 0`, `visited["rolodex.interact.vouch"] > 0`). `class TestBurnoutAvoider(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`burnout_avoider(seed=23, years=40)`) and `test_disappears_at_least_once` (fresh `Counter()`, `years=40`, asserts `visited["leverage.disappear"] > 0` and `result["disappear_count"] > 0`). Also add one final cross-archetype sanity test in a new `class TestAllEightArchetypesExist(unittest.TestCase):` — `test_archetype_names_manifest_is_fully_resolvable` imports `_decision_coverage.ARCHETYPE_NAMES` and `_archetype_policies`, asserts `all(hasattr(_archetype_policies, name) and callable(getattr(_archetype_policies, name)) for name in ARCHETYPE_NAMES)` — this is the concrete proof that Plan 05's `run_full_coverage_check()` will not hit an `AttributeError` when it runs for real.
  </action>
  <verify>
    <automated>python3 -m unittest -v callback.engine.tests.test_archetype_policies</automated>
  </verify>
  <acceptance_criteria>`python3 -m unittest -v callback.engine.tests.test_archetype_policies` exits 0 with all 8 archetype TestCase classes plus TestAllEightArchetypesExist passing; `python3 -m unittest discover callback/engine/tests` still exits 0.</acceptance_criteria>
  <done>All 8 archetype TestCase classes exist and pass; TestAllEightArchetypesExist confirms every ARCHETYPE_NAMES entry resolves to a callable; the full existing suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | Same offline, single-process, stdlib-only engine as Plans 01-03 — this plan only appends two pure-Python functions and their tests to existing internal modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | N/A (all STRIDE categories) | _archetype_policies.py (family_first, burnout_avoider) | low | accept | No trust boundary exists; ASVS V2/V3/V4/V6 not applicable. No package installs in this plan. |
</threat_model>

<verification>
`python3 -m unittest discover callback/engine/tests` must pass in full. `python3 -m callback.engine.simulation._decision_coverage` now runs against a complete 8-archetype set for the first time — expect a small remaining gap list (director.franchise.reboot_pitch/spinoff_pitch and any long-tail items are the likely survivors), which is exactly what Plan 05 exists to close.
</verification>

<success_criteria>
All 8 required archetypes (prestige_chaser, franchise_maximizer, indie_purist, risk_averse, gambler, director_track, family_first, burnout_avoider) exist in `_archetype_policies.py`, satisfying POLICY-01's minimum; `_decision_coverage.ARCHETYPE_NAMES` fully resolves; the test suite is green.
</success_criteria>

<output>
Create `.planning/phases/01-decision-space-playtest-policy-coverage/01-04-SUMMARY.md` when done.
</output>
