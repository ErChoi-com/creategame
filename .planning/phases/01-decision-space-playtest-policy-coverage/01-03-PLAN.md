---
phase: 01-decision-space-playtest-policy-coverage
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - callback/engine/simulation/_archetype_policies.py
  - callback/engine/tests/test_archetype_policies.py
autonomous: true
requirements: [POLICY-01, POLICY-02]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "director_track runs a full 60-year simulated career end-to-end without raising and returns a plain dict."
    - "Across a 60-year run, director_track visits every director-only decision point still uncovered after Plans 01-02: budget tiers mid/tentpole, self_financed projects, franchise sequel/reboot/spinoff pitches, all 8 adaptation source types, adaptation licensing fraction, push_director_deal_for_backend, director script notes clarity/ambiguity, director release festival/streaming/shelved, and dev actions self_finance/drawer."
    - "director_track's franchise-reboot strategy deliberately lets a self-started franchise go dormant past REBOOT_MIN_DORMANT_YEARS (5) before pitching a reboot, rather than continuously re-sequeling it — this is the single hardest-to-reach decision point in the whole catalog and needs its own dedicated long-horizon logic, not a copy of any existing tendency's yearly loop."
  artifacts:
    - "callback/engine/simulation/_archetype_policies.py (adds director_track)"
    - "callback/engine/tests/test_archetype_policies.py (adds TestDirectorTrack)"
  key_links:
    - "director_track's franchise_id continuity tracking must be independent of franchise_maximizer's (Plan 01) — they run in separate Session instances/seeds, sharing only the ID vocabulary, not any runtime state."
    - "Every visited Counter key director_track increments must match decision-map.md's director.* ID vocabulary from Plan 01 exactly."
---

<objective>
Add the standalone `director_track` archetype to `_archetype_policies.py` — the single archetype responsible for closing every remaining director-only coverage gap (Coverage Gap Inventory items 17-28: budget tiers, self-financing, franchise sequel/reboot/spinoff pitching, adaptation source variety, licensing fraction, backend deal push, director script notes, director release variety, and the two never-used dev actions).

Purpose: This is the densest, most novel archetype in the phase — no existing report script tracks franchise continuity from the director's chair or deliberately lets a franchise go dormant to earn a reboot pitch, so it gets its own isolated plan rather than being folded alongside simpler archetypes.
Output: `director_track()` appended to `_archetype_policies.py`; `TestDirectorTrack` appended to `test_archetype_policies.py`.
</objective>

<artifacts_produced>
- `_archetype_policies.py`: `director_track()`.
- `test_archetype_policies.py`: `TestDirectorTrack`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-decision-space-playtest-policy-coverage/01-CONTEXT.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-RESEARCH.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-PATTERNS.md
@callback/engine/simulation/_archetype_policies.py
@callback/engine/simulation/_full_data_report.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: director_track - the dedicated director-mode archetype closing all remaining director-only gaps</name>
  <files>callback/engine/simulation/_archetype_policies.py</files>
  <read_first>
    01-RESEARCH.md's Director Mode table in full (every row from "Become director" through "Directing spin-off pitch") and its "Gated Decision Points" table rows for `directing_franchise_options`/`directing_reboot_options`/`directing_spinoff_options` (SEQUEL_ELIGIBLE_MAX_DORMANT_YEARS=4, REBOOT_MIN_DORMANT_YEARS=5); `session.py`'s `start_directing_project(genre, budget_tier, self_financed=False, franchise_id=None, new_franchise=False, cast_decision=None)` full docstring and signature (already read in full this planning session — franchise_id overrides genre to the franchise's own, new_franchise registers a fresh property on first resolve, cast_decision is "recast"/"write_out"/None and only meaningful alongside franchise_id); `directing_franchise_options()`, `directing_reboot_options()`/`pitch_reboot(franchise_id)`, `directing_spinoff_options()`/`launch_directing_spinoff(franchise_id)` (all four read in full this session — reboot requires the franchise to already be in `state.retired_franchises`, which only happens via the background `decay_dormant_franchises` process, not anything the player triggers directly — meaning director_track must simply NOT re-pitch a sequel to its own started franchise for at least 5+ years after its last installment and let the background decay carry it into `retired_franchises` on its own); `choose_adaptation_source_type`/`adaptation_source_type_options`/`choose_adaptation_licensing_fraction`; `push_director_deal_for_backend`; `director_script_note_options`/`choose_director_script_note_action`; `director_release_options`/`request_director_release_strategy`; `director_dev_action_options`/`DEV_ACTIONS` (self_finance, drawer are the two never-used ones).
  </read_first>
  <action>
    Add `def director_track(seed: int, years: int = 60, visited: Counter | None = None) -> dict:` to `_archetype_policies.py` (append after `gambler`). This archetype calls `session.become_director()` and drives ONLY the directing loop every year (no `offer_board()`/`accept()` acting-side calls at all — this archetype's whole point is exhausting the director-only decision space, matching how `_director_report.py` was director-focused before being superseded, per 01-RESEARCH.md's "State of the Art" table).

    Franchise strategy (the archetype's hardest and most novel piece — track this with local state across the whole `years` loop, not per-year): maintain `own_franchise_id: str | None = None` and `own_franchise_last_pitch_year: int | None = None`. In year 1 (or the first year `can_start_new_project` is true), start a brand-new franchise via `start_directing_project(genre, "low", new_franchise=True)`, then set `own_franchise_id` from `session.director_status()`'s newly-created project once it resolves (cross-reference the project's own `franchise_id`/genre in the status dict — confirm the exact field name by reading `director_status()`'s return shape before writing this). For roughly the next `SEQUEL_ELIGIBLE_MAX_DORMANT_YEARS`-worth of years, pitch ONE sequel to `own_franchise_id` via `directing_franchise_options()` -> `start_directing_project(franchise_id=own_franchise_id, budget_tier=...)` (tally `visited["director.franchise.sequel_pitch"] += 1`) using `cast_decision="recast"` on one pitch and `"write_out"` on another (each tallied as `visited["director.project.cast_decision.recast"] += 1` / `"...write_out"`) to close Coverage Gap Inventory item 19's cast_decision half. After that initial run of sequels, STOP pitching to `own_franchise_id` entirely (a deliberate, commented no-op — "letting the franchise go dormant on purpose so decay_dormant_franchises eventually retires it and pitch_reboot becomes reachable") and switch to original/adaptation projects for the remaining years. From year `own_franchise_last_pitch_year + REBOOT_MIN_DORMANT_YEARS` onward, check `session.directing_reboot_options()` every year; the moment `own_franchise_id` appears in that list, call `session.pitch_reboot(own_franchise_id)` (tally `visited["director.franchise.reboot_pitch"] += 1` regardless of the boolean outcome — the *request* is what decision-map.md's semantics count, not whether the studio said yes) — note in a comment that `pitch_reboot` is a real probabilistic ask (`reboot_probability`) so it may need to be retried across several years if it fails; keep retrying until either it succeeds or `years` runs out. Separately, once ANY franchise (own or otherwise, read via `session.directing_spinoff_options()`) clears `SPINOFF_INDISPENSABILITY_THRESHOLD`, call `session.launch_directing_spinoff(franchise_id)` on the first eligible one and tally `visited["director.franchise.spinoff_pitch"] += 1`.

    Budget tiers: cycle deterministically through all 5 (`micro, low, mid, upper, tentpole`) across successive `start_directing_project` calls using a running project-count index modulo 5 (tally `visited[f"director.budget_tier.{tier}"] += 1` each time) — closes Coverage Gap Inventory item 17's `mid`/`tentpole` gap (the other 3 tiers are already covered by existing scripts, tally them too for this archetype's own accuracy). Self-financing: on roughly every 5th new project (`project_count % 5 == 0`), pass `self_financed=True` instead of the normal studio-attached path (tally `visited["director.project.self_financed"] += 1` — closes item 18); document that a self-financed project resolves immediately on the next action taken (per the docstring already read), so this archetype's dev-action loop must not assume multiple quarters of runway on that project.

    Adaptation: on projects that are NOT franchise sequels/reboots/spinoffs, call `session.choose_adaptation_source_type(source_type)` cycling deterministically through all values NOT equal to the default (`public_domain, foreign_remake, stage_play, true_story, comic, video_game, toy_line` — 7 values, `novel` is already the default every existing script implicitly uses) via a running index (tally `visited[f"director.adaptation.source_type.{source_type}"] += 1`), and on alternating projects call `session.choose_adaptation_licensing_fraction(0.8)` (a deliberate non-default value — tally `visited["director.adaptation.licensing_fraction"] += 1`) before the `"option_adaptation"` dev action is actually taken — this closes Coverage Gap Inventory items 20 and 21.

    Per-quarter dev-action loop (mirrors `_full_data_report.py`'s directing block's overall shape but with an explicit action schedule, NOT the diluted `tick % N` modulo-cycling Pitfall 2 documents as the actual cause of `self_finance`/`drawer` never firing in every existing script): maintain a per-project `actions_taken: list[str]` and explicitly ensure `"self_finance"` fires at least once (when a project's `momentum_band` is `"fading"` or worse and net worth allows it — tally `visited["director.dev_action.self_finance"] += 1`) and `"drawer"` fires at least once (when a project's `momentum_band` is `"dead"` and it's cheaper to shelve than scrap — tally `visited["director.dev_action.drawer"] += 1`) across the run, rather than relying on a diluted cycle list to eventually land on them by chance — closes Coverage Gap Inventory item 25. Also explicitly call `session.push_director_deal_for_backend(project_index, bonus_type)` once per project when `director_deal_available()` and `director_box_office_bonus_available()` are both true (tally `visited["director.deal.backend_push"] += 1` — closes item 22), and call `session.choose_director_script_note_action(project_index, "clarity")` on one project and `"ambiguity"` on another (tally `visited["director.script_note.clarity"] += 1` / `"...ambiguity"` — closes item 23).

    Release: request `"festival"` on one project, `"streaming"` on another, `"shelved"` on a third, `"wide"`/`"limited"` on the rest (tally `visited[f"director.release.request.{strategy}"] += 1` before each `request_director_release_strategy` call — closes item 24). Return the same plain-dict shape as the other archetypes, extended with `"own_franchise_id": own_franchise_id` and `"reboot_pitched": bool` for debugging visibility.
  </action>
  <verify>
    <automated>python3 -c "from collections import Counter; from callback.engine.simulation._archetype_policies import director_track; v = Counter(); r = director_track(seed=13, years=60, visited=v); assert isinstance(r, dict) and r.get('age', 0) > 22, r; needed = ['director.budget_tier.mid', 'director.budget_tier.tentpole', 'director.project.self_financed', 'director.deal.backend_push', 'director.script_note.clarity', 'director.script_note.ambiguity', 'director.dev_action.self_finance', 'director.dev_action.drawer']; missing = [k for k in needed if v[k] == 0]; assert not missing, missing; print('OK', len(v), 'ids visited')"</automated>
  </verify>
  <acceptance_criteria>director_track runs the full 60-year window for seed 13 without raising; all 8 IDs listed in the verify command's `needed` list are visited at least once; the returned dict contains "own_franchise_id" and "reboot_pitched" keys.</acceptance_criteria>
  <done>director_track() exists in _archetype_policies.py, runs end-to-end for a full 60-year career, and closes Coverage Gap Inventory items 17-25 reliably within one 60-year run. Items 26-28 (sequel/reboot/spinoff pitches) are exercised opportunistically per the franchise strategy above but may need a longer years window or multiple seeds to land within any single run — flag this explicitly in the plan's SUMMARY.md if seed 13 at years=60 does not reach director.franchise.reboot_pitch, since Plan 05's coverage-closure pass must know to widen the seed/years sampling for this specific ID rather than treat it as a design failure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Smoke tests for director_track</name>
  <files>callback/engine/tests/test_archetype_policies.py</files>
  <read_first>
    The existing test classes in `test_archetype_policies.py` from Plans 01-02 (read the current file to match its exact style before appending).
  </read_first>
  <action>
    Append to `callback/engine/tests/test_archetype_policies.py` (extend the import line to add `director_track`): `class TestDirectorTrack(unittest.TestCase):` with `test_runs_to_completion_without_raising` (`director_track(seed=13, years=60)` — a full 60-year run is deliberate here, unlike the other archetypes' shorter smoke windows, since several of this archetype's decision points (reboot pitch) are only reachable on a long horizon) and `test_covers_budget_tiers_and_dev_actions` (fresh `Counter()`, `years=60`, asserts every ID in the same `needed` list used in Task 1's verify command is present with count > 0 — duplicate that exact list here rather than importing it from a script, so the test is self-contained and independently readable) and `test_attempts_franchise_reboot_across_seeds` (loops seeds `[13, 27, 41]`, `years=60` each, breaks on the first seed where `visited["director.franchise.reboot_pitch"] > 0`; if none of the three seeds reach it, the test should `self.skipTest(...)` with a message explaining Coverage Gap Inventory item 27 needs Plan 05's dedicated coverage-closure pass to widen sampling further — do NOT let this test silently pass on a false premise, and do NOT hard-fail Plan 03 over a single archetype's hardest-to-reach decision point when Plan 05 exists specifically to close exactly this kind of remaining gap).
  </action>
  <verify>
    <automated>python3 -m unittest -v callback.engine.tests.test_archetype_policies</automated>
  </verify>
  <acceptance_criteria>`python3 -m unittest -v callback.engine.tests.test_archetype_policies` exits 0 (a skipped `test_attempts_franchise_reboot_across_seeds` counts as a pass, not a failure); `python3 -m unittest discover callback/engine/tests` still exits 0.</acceptance_criteria>
  <done>TestDirectorTrack exists and passes (or explicitly, legibly skips the reboot-specific sub-test with a clear pointer to Plan 05); the full existing suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | Same offline, single-process, stdlib-only engine as Plans 01-02 — this plan only appends one pure-Python function and its tests to existing internal modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | N/A (all STRIDE categories) | _archetype_policies.py (director_track) | low | accept | No trust boundary exists; ASVS V2/V3/V4/V6 not applicable. No package installs in this plan. |
</threat_model>

<verification>
`python3 -m unittest discover callback/engine/tests` must pass in full. `python3 -m callback.engine.simulation._decision_coverage` should show the gap list shrunk further, with only family_first/burnout_avoider's ~4 remaining IDs (Plan 04) and possibly `director.franchise.reboot_pitch` / `director.franchise.spinoff_pitch` (Plan 05's explicit job) left.
</verification>

<success_criteria>
`_archetype_policies.py` contains 6 of the 8 required archetypes; director_track reliably closes Coverage Gap Inventory items 17-25 within a single 60-year run at a fixed seed, and makes a genuine, documented attempt at items 26-28 even if they need Plan 05's wider sampling to confirm; the test suite is green.
</success_criteria>

<output>
Create `.planning/phases/01-decision-space-playtest-policy-coverage/01-03-SUMMARY.md` when done.
</output>
