---
phase: 01-decision-space-playtest-policy-coverage
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - callback/engine/simulation/_archetype_policies.py
  - callback/docs/design/decision-map.md
  - callback/engine/tests/test_decision_coverage.py
autonomous: true
requirements: [MAP-01, POLICY-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running _decision_coverage.run_full_coverage_check() across the full 8-archetype set at a fixed, documented seed set and years=60 returns an empty gap list — zero decision points cataloged in decision-map.md go untested, satisfying POLICY-02's explicit bar."
    - "Every decision-map.md row's ID is visited by at least one archetype in the coverage run; no row was silently dropped or renamed without updating the corresponding archetype's Counter key."
    - "python3 -m unittest discover callback/engine/tests passes in full, confirming this additive-only phase left every pre-existing test untouched."
  artifacts:
    - "callback/engine/tests/test_decision_coverage.py (tightens TestCoverageIntegration to a hard zero-gap assertion — the phase's actual gate test)"
    - "callback/engine/simulation/_archetype_policies.py (only if gap-closure tweaks are needed)"
    - "callback/docs/design/decision-map.md (only if a citation/ID inconsistency is found during the closure pass)"
  key_links:
    - "This plan is where POLICY-02's 'gaps are not deferred to Phase 4' rule (01-CONTEXT.md's Coverage Verification decision) is actually enforced — any gap found here gets fixed within this task, not logged for later."
    - "The final zero-gap test in test_decision_coverage.py is the concrete phase-gate the ROADMAP's Phase 1 success criterion #3 (cross-referencing shows zero gaps) reduces to."
---

<objective>
Run the completed 8-archetype set through `_decision_coverage.run_full_coverage_check()` for real, close any decision point still showing zero visits by adjusting the responsible archetype's logic or seed/years sampling (never by weakening the catalog or the coverage tool), and lock the result in with a hard zero-gap test — the concrete verification of POLICY-02's "zero decision points go untested" bar and ROADMAP Phase 1's third success criterion.

Purpose: Plans 01-04 built the catalog and all 8 archetypes on the assumption that their individually-designed coverage would add up to zero gaps; this plan is where that assumption gets checked against the real coverage tool and corrected if it's wrong, per CONTEXT.md's explicit instruction that unreached decision points are fixed within this phase, not deferred.
Output: A passing `test_decision_coverage.py` test asserting `run_full_coverage_check(seeds=[...], years=60) == []`, plus whatever small `_archetype_policies.py`/`decision-map.md` corrections were needed to get there.
</objective>

<artifacts_produced>
No new symbols — this plan closes out the phase by exercising and, where needed, correcting the artifacts Plans 01-04 already produced (`_archetype_policies.py`'s 8 functions, `decision-map.md`'s catalog, `_decision_coverage.py`'s tool — the latter is not expected to need changes, since its contract was finalized in Plan 01).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-decision-space-playtest-policy-coverage/01-CONTEXT.md
@.planning/phases/01-decision-space-playtest-policy-coverage/01-RESEARCH.md
@callback/engine/simulation/_decision_coverage.py
@callback/engine/simulation/_archetype_policies.py
@callback/docs/design/decision-map.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full coverage check, diagnose every remaining gap</name>
  <files>callback/engine/simulation/_decision_coverage.py</files>
  <read_first>
    `callback/engine/simulation/_decision_coverage.py` (the `run_full_coverage_check`/`check_coverage`/`load_expected_ids` contract, as finalized in Plan 01 — do not change its signatures in this task, only use it); `callback/engine/simulation/_archetype_policies.py` in full (all 8 archetypes as they stand after Plans 01-04); `callback/docs/design/decision-map.md` in full (the ID vocabulary every archetype is supposed to be tallying against); 01-CONTEXT.md's Coverage Verification decision ("any decision point unreachable by the current archetype set gets a policy fix... within this phase, per POLICY-02's explicit 'zero decision points go untested' bar. Gaps are not deferred to Phase 4.").
  </read_first>
  <action>
    Run `python3 -m callback.engine.simulation._decision_coverage` (its `__main__` block, built in Plan 01) using a documented seed set of at least 3 seeds per archetype (reuse the seeds each archetype's own smoke test already uses in `test_archetype_policies.py` — e.g. `[1, 3, 5, 8, 9, 13, 17, 23]`, one representative per archetype, plus 1-2 extra seeds for the archetypes flagged in Plan 03 as needing wider sampling for `director.franchise.reboot_pitch`/`director.franchise.spinoff_pitch`) and `years=60`. For every ID still in the returned gap list, diagnose the root cause using the same three checks Pitfalls 2/4/5 in 01-RESEARCH.md describe: (1) is the archetype's Counter key a typo that doesn't match decision-map.md's ID exactly (compare byte-for-byte, not by eye) — if so, fix the typo in the archetype code, not the doc; (2) is the decision point genuinely gated behind a threshold the sampled seeds/years never clear (e.g. `director.franchise.reboot_pitch` needing `own_franchise_last_pitch_year + 5` years of dormancy before end of a 60-year run) — if so, widen that specific archetype's sampling (more seeds, or a `years` override just for the coverage-check invocation, not for its own smoke test) until it reliably clears; (3) is the archetype's own logic simply not calling the Session method it was assigned in Plans 01-04 (a genuine implementation gap, not a sampling problem) — if so, fix the archetype's logic to actually call it. Do not weaken `check_coverage()`'s logic, do not remove rows from decision-map.md, and do not mark any ID as permanently excluded to make the test pass — every gap gets a real fix per CONTEXT.md's explicit rule. If, after genuine investigation, any single ID proves to require materially more engineering than a targeted logic/sampling fix (a new architectural capability, not a tuning tweak), stop and report it back rather than silently declaring victory — this is the one legitimate case where the phase's zero-gap bar might need the orchestrator's attention before this plan can close.
  </action>
  <verify>
    <automated>python3 -c "from callback.engine.simulation._decision_coverage import run_full_coverage_check; gaps = run_full_coverage_check(seeds=[1, 3, 5, 8, 9, 13, 17, 23, 27, 41], years=60); print('GAPS:', gaps)"</automated>
  </verify>
  <acceptance_criteria>The verify command's printed GAPS list is documented in the task's own commit message or SUMMARY.md (even if non-empty at this point — Task 2 is where fixes land); every gap in that list has an identified root cause (typo / sampling / logic gap) recorded before Task 2 begins.</acceptance_criteria>
  <done>The full 8-archetype coverage run has been executed and every remaining gap (if any) has a documented root cause ready for Task 2's fix.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Close every diagnosed gap and finalize the zero-gap test</name>
  <files>callback/engine/simulation/_archetype_policies.py, callback/docs/design/decision-map.md, callback/engine/tests/test_decision_coverage.py</files>
  <read_first>
    Task 1's diagnosis output; the specific archetype function(s) implicated for each gap (re-read only the implicated function's current source, not the whole file again).
  </read_first>
  <action>
    Apply the targeted fix for each gap diagnosed in Task 1: correct Counter-key typos in `_archetype_policies.py` (never in `decision-map.md` — the doc is the source of truth, the code must match it); widen seed lists or per-call `years` for archetypes whose decision points are threshold-gated (adjust only the specific archetype function's own internal logic if it needs more attempts within the run, e.g. retry `pitch_reboot` every year once eligible rather than once); add the missing Session call to any archetype whose logic simply never reached an assigned decision point. Re-run the same `run_full_coverage_check(seeds=[...], years=60)` call from Task 1's verify command after each fix until it returns `[]`. Once it does, replace `test_decision_coverage.py`'s `TestCoverageIntegration.test_prestige_chaser_visits_are_all_known_ids` test class scope by ADDING a new method `test_full_archetype_set_has_zero_gaps` to `TestCoverageIntegration` — the phase's actual gate test: `from callback.engine.simulation._decision_coverage import run_full_coverage_check; gaps = run_full_coverage_check(seeds=[1, 3, 5, 8, 9, 13, 17, 23, 27, 41], years=60); self.assertEqual(gaps, [], f"uncovered decision points: {gaps}")` — using the exact same seed set that verified zero gaps in this task, hard-coded (not re-derived), so the test is deterministic and reproducible. Do a final consistency pass over `decision-map.md`: spot-check at least 10 rows across different sections against their cited source file/function to confirm the citation is still accurate (grep the cited function name in the cited file), correcting any row found stale.
  </action>
  <verify>
    <automated>python3 -m unittest -v callback.engine.tests.test_decision_coverage && python3 -m unittest discover callback/engine/tests</automated>
  </verify>
  <acceptance_criteria>`run_full_coverage_check(seeds=[1, 3, 5, 8, 9, 13, 17, 23, 27, 41], years=60)` returns `[]`; `test_full_archetype_set_has_zero_gaps` exists in `TestCoverageIntegration` and passes; the full existing test suite (117+ pre-existing tests plus every test file added across Plans 01-04) passes in full with zero failures.</acceptance_criteria>
  <done>Every decision point cataloged in decision-map.md is visited by at least one of the 8 archetypes across the documented seed set; test_decision_coverage.py's test_full_archetype_set_has_zero_gaps is the durable, reproducible proof of this and is committed as part of the suite; decision-map.md's spot-checked citations are accurate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | Same offline, single-process, stdlib-only engine as Plans 01-04 — this plan only runs existing code, applies small targeted corrections to it, and tightens one test's assertion. No new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | N/A (all STRIDE categories) | _archetype_policies.py, decision-map.md, test_decision_coverage.py | low | accept | No trust boundary exists; ASVS V2/V3/V4/V6 not applicable. No package installs in this plan. |
</threat_model>

<verification>
`python3 -m unittest discover callback/engine/tests` must pass in full — this is the phase-gate command per 01-VALIDATION.md's Sampling Rate ("Before /gsd-verify-work: ... must be fully green ... AND the coverage report must show zero gaps"). `python3 -m callback.engine.simulation._decision_coverage` (its `__main__` block) must print `OK - zero gaps` at the documented seed set.
</verification>

<success_criteria>
`_decision_coverage.run_full_coverage_check()` returns an empty list across the documented seed set — POLICY-02's "zero decision points go untested" bar is met and locked in by a committed, deterministic test; ROADMAP Phase 1's three success criteria are all satisfied: (1) decision-map.md catalogs every decision point with source citations, (2) 8 distinct archetype policies exist extending the existing report infrastructure, (3) cross-referencing shows zero gaps; the full existing engine test suite remains green, confirming this phase was purely additive.
</success_criteria>

<output>
Create `.planning/phases/01-decision-space-playtest-policy-coverage/01-05-SUMMARY.md` when done.
</output>
